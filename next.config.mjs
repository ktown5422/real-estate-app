/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        unoptimized: true,
        domains: ['ygrikubhyscffvawjpul.supabase.co', 'img.clerk.com']
    }
};

export default nextConfig;
